{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // Define o diretório base para resolução de módulos
    "paths": {
      "@/*": [
        "my-app/src/*"
      ]
    }
  },
  "include": [
    "my-app/**/*.ts",
    "my-app/**/*.tsx"
  ]
}
